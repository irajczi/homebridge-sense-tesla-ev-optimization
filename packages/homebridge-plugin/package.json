{
  "name": "homebridge-ev-solar-charger",
  "version": "0.9.0",
  "description": "Homebridge plugin that uses Sense Solar monitoring to optimize Tesla EV charging based on solar production",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "rimraf ./dist && tsc",
    "watch": "tsc --watch"
  },
  "engines": {
    "node": ">=22.0.0",
    "homebridge": ">=2.0.0"
  },
  "keywords": [
    "homebridge-plugin",
    "tesla",
    "sense",
    "solar",
    "ev",
    "charging",
    "homebridge"
  ],
  "peerDependencies": {
    "homebridge": ">=2.0.0"
  },
  "dependencies": {
    "@homebridge-ev-solar-charger/core": "*"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "homebridge": "^2.0.0",
    "rimraf": "^5.0.0",
    "typescript": "^5.0.0"
  }
}
